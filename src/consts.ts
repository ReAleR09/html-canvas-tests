import { Color } from "./color";

export const BG_COLOR = new Color(0, 0, 0);
export const OBJECT_COLOR = new Color(0, 255, 0);

export const REFRESH_RATE = 5000;

export const IS_CHECKERBOARD_ENABLED = false;
export const WEB_WORKERS = 4;

export const FPS_MEASURE_COUNTER = 60;